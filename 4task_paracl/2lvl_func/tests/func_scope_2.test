a = ?;
b = ?;

sum = func(a, b) : summarize {
	a + b;
}

if (a > 0 && b > 0) {
	sub = func(a, b) : subtract {
		a - b;
	}

	print sum(a, b);
	print summarize(b - a, b);
	print sub(a, b);
	print subtract(b - a, b);
}